// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
	production: false,
	// API_BASE_URL: 'https://kanban-back.onrender.com',
	API_BASE_URL: 'https://blooming-brook-64283.herokuapp.com',
	SOCKET_ENDPOINT: 'https://blooming-brook-64283.herokuapp.com',
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error-toast stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error-toast is thrown.
 */
// import 'zone.js/plugins/zone-error-toast';  // Included with Angular CLI.
